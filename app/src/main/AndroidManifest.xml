<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Yoloandroid"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name="ch.seccom.omate.MainActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|uiMode"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Android App Links: open https://o-mate.app links in the app instead of a
                 browser. Auto-verification requires /.well-known/assetlinks.json on the
                 domain (see frontend/public/.well-known/assetlinks.json). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- All o-mate.app pages deep-link into the app — no per-route allow-list.
                     The calendar feed is served from the API host (admin.o-mate.app), a
                     DIFFERENT host that is not matched here, so webcal/.ics links are never
                     claimed by the app. (In-app .ics taps are additionally intercepted in
                     MainActivity.handleUrl.) Keep the calendar feed off the o-mate.app host. -->
                <data android:scheme="https" android:host="o-mate.app" />
                <data android:scheme="https" android:host="www.o-mate.app" />
            </intent-filter>
        </activity>
    </application>

</manifest>